import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // Leave empty for standard default Edge caching and routing,
  // or add Cloudflare-specific overrides here when needed.
});